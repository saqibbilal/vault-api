name: Sync Dev to Main

on:
  push:
    branches:
      - dev

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create/Auto-merge PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # find existing PR
          PR=$(gh pr list \
            --base main \
            --head dev \
            --state open \
            --json number \
            -q '.[0].number')

          # create if missing
          if [ -z "$PR" ]; then
            gh pr create \
              --base main \
              --head dev \
              --title "🚀 Sync dev → main" \
              --body "Automated merge from dev" \
              --label automerge
            PR=$(gh pr list --base main --head dev --json number -q '.[0].number')
          fi

          # enable auto merge
          gh pr merge "$PR" --auto --merge
